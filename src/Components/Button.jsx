import React from 'react'

const Button = ({btnName}) => {
 return (
  <button style={{backgroundColor: '#728BAD', color: 'white', padding:'.7rem 1.3rem', borderRadius:'.6rem'}}>{btnName}</button>
 )
}

export default Button